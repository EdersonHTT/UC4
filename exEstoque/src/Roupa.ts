export class Roupa {
  descricao: string;
  tamanho: number;
  preco: number;

  constructor(descricao: string, tamanho: number, preco: number) {
      this.descricao = descricao;
      this.tamanho = tamanho;
      this.preco = preco;
  }
}